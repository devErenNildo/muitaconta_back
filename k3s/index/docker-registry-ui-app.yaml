apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: docker-registry-ui
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/devErenNildo/muita-conta_devops.git'
    path: infra/docker-registry-ui
    targetRevision: HEAD
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: docker-registry-ui
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true